# Codex

Placeholder. Codex setup (`AGENTS.md`, skills) is not started. Skills in `claude/plugins/*/skills` follow the vendor-neutral Agent Skills spec (agentskills.io), so they can be referenced from here rather than copied.
